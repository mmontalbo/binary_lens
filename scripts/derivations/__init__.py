"""Derivation subsystems for `binary_lens`.

This package contains the logic that turns collected facts (call edges, strings,
callsites) into higher-level lenses like CLI surfaces and other derived payloads.
"""
