"""Collector subsystems for `binary_lens`.

This package holds the implementation that historically lived in
`scripts/export_collectors.py`. The top-level module remains as a shim to keep
existing import paths (notably the Ghidra exporter script) stable.
"""

