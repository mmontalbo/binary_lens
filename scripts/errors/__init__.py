"""Error/exit lens subsystems (Milestone 2).

The implementation was split out of `scripts/export_errors.py` to keep modules
small and make the data-flow for each emitted artifact clearer. The original
`export_errors` module remains as a shim for backwards-compatible imports.
"""

