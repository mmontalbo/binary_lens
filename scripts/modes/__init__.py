"""Mode export implementation (Milestone 3).

This package is split out of `scripts/export_modes.py` to keep individual subsystems
small and navigable. Public entrypoints remain re-exported from `export_modes.py` for
backwards compatibility with existing Ghidra scripts.
"""

